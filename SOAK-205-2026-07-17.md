# Soak / Endurance #205 — 2026-07-17 (5.4h)

## Veredito

**Sem leak de memória / fd / conexão e ZERO crash no core HTTP do v2 sob carga
sustentada por 5,4 horas.** Fecha o DoD de "horas" do #205 — a primeira janela
contínua de **horas** (runs anteriores morriam em ~49 min por lifetime da WSL).

## Configuração

- Harness: `Benchmark/infra/soak-containers.sh 18000 300 soakNight` — app +
  sampler + k6 como containers **detached sob dockerd** (independem de qualquer
  `wsl.exe` vivo; foi o que destravou a duração de horas).
- Imagem: `bench-app:latest` (build 2026-07-16, pós-fixes de julho).
- Carga (k6, `bench-soak-lifetime.js`): `noConnectionReuse:true`, alvo 300 iters/s,
  **15% pela via de exceção** (`/error/500`) — martela accept→serve→close→free +
  o `Release` do refcount na EXCEÇÃO (os fixes de lock/refcount de julho).
- Sampler: anon do cgroup v2 (`memory.stat`), rss, fd (`/proc/1/fd`), estab, a
  cada 5s → `soak-out/soakNight.csv` (3622 amostras).
- Escopo: `/ping` puro (sem DB) — isola o core HTTP/conexão/io_uring.

## Resultados

### Carga (k6)
- **http_reqs: 5.404.455** em ~5h (299,25/s sustentado).
- **http_req_failed: 0,00% (0 de 5.404.455).**
- checks: **100,00%** (5.404.455/5.404.455) — `ping 200` + `err-path 500`.
- Latência: avg **3,59 ms**, med 4,11 ms, p95 **5,11 ms**, max 630 ms.
  Threshold `p(95)<3000ms` PASSOU.
- dropped_iterations: 58 (0,003/s — rate-limiter do k6, não falha do servidor).
- Tráfego: 1,8 GB recebidos / 512 MB enviados.

### Memória / recursos (sampler + soak-analyze.py)
- **anon: início 2,14 → pico 4,52 → fim 4,51 MiB.** Sobe no warmup (~22 min) e
  depois **PLATÔ**.
- **Regressão linear pós-warmup: 0,000 MiB/h** (delta absoluto **+0,0 KiB em
  259 min**).
- **VEREDITO soak-analyze.py: PLATÔ / working-set saudável (sem leak aparente).**
- rss: pico = fim = 21.456 kB (flat).
- fd: min 7, pico 10, fim 8 — estável, sem leak.
- Zero erro/exceção/crash no log do `soak-app` durante as 5,4 h.

## Interpretação

O caminho de exceção (`/error/500`) foi exercitado ~810k vezes (15% de 5,4 M) sem
qualquer vazamento — confirma que o `Release` do refcount na exceção (#213) e o
lifecycle accept→serve→close→free do backend io_uring/epoll são estáveis sob
carga prolongada. O anon flat por 4,3 h pós-warmup descarta leak lento.

## Lacunas remanescentes (não bloqueiam)

- Escopo HTTP puro (`/ping`): soak do app completo com CRUD/Postgres/NFCe ainda
  não coberto (ruído conhecido de pooling FireDAC/libpq, camada compartilhada).
- `ReportMemoryLeaksOnShutdown` não capturado (parada = SIGTERM, sem finalização
  RTL completa) — mas os contadores de runtime (anon/fd/rss) já são a evidência
  forte de ausência de leak.

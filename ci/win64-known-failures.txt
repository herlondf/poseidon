# Known-failing Win64 integration tests. CI tolerates exactly these; any NEW
# failure fails the build.
#
# 2026-07-16: the Windows I/O backend now SERVES end-to-end (16 of the previous
# 19 "socket hang up" failures fixed — IOCP is the default and loads AcceptEx via
# a static mswsock fallback; see Poseidon.Net.IO.IOCP). These 3 remaining are
# REAL behavioural bugs on the IOCP path (not environmental), tracked for fixing:
#   - Get_HandlerSetsStatus_ReturnsOverriddenStatus  (invalid server response)
#   - KeepAlive_MultipleRequests_ReuseConnection      (2nd keep-alive req no reply)
#   - MaxRequestSize_OversizedBody_Returns413         (returns 400 instead of 413)
Get_HandlerSetsStatus_ReturnsOverriddenStatus
KeepAlive_MultipleRequests_ReuseConnection
MaxRequestSize_OversizedBody_Returns413
